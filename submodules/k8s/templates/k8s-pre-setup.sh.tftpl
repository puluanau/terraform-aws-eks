#! /usr/bin/env bash

RED="\e[31m"
GREEN="\e[32m"
EC="\e[0m"

source ${k8s_functions_sh_filename}

main() {
  open_ssh_tunnel_to_k8s_api
  check_kubeconfig
  set_k8s_auth
  install_calico
}

trap close_ssh_tunnel_to_k8s_api EXIT
main
